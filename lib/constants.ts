// Default printing cost when product doesn't specify one
export const DEFAULT_PRINTING_COST = 15;

// Obfuscated admin path – set NEXT_PUBLIC_ADMIN_PATH in .env to customize
export const ADMIN_PATH = process.env.NEXT_PUBLIC_ADMIN_PATH || "/cp";
